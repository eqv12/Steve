class Dino {
  constructor(path, task, seqNum) {
    // initialize properties
    this.path = path;
    this.task = task;
    this.seqNum = seqNum
  }

  methodOne() {
    // instance method
  }

  methodTwo(param) {
    // use this.prop1 / this.prop2
  }
}
